spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/dmall?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: root

mybatis-plus:
  type-aliases-package: com.dg.entity.domain
  configuration:
    map-underscore-to-camel-case: true

swagger:
  enabled: true

base:
  tableSchema: "dmall"